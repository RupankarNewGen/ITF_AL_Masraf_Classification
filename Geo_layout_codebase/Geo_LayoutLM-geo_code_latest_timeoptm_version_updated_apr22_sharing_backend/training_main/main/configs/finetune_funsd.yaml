# For workspace name
# The checkpoint file location to be saved: {workspace}/checkpoints/
# The log file location to be saved: {workspace}/tensorboard_logs/
workspace: /datadrive2/IDF_AL_MASRAF/ITF_Al_Masraf_Classification/Geo_layout_codebase/Geo_LayoutLM-geo_code_latest_timeoptm_version_updated_apr22_sharing_backend/training_main/main/CS_Data/result/geo_trained_on_almasraf_cs

dataset: custom
dataset_root_path: /datadrive2/IDF_AL_MASRAF/ITF_Al_Masraf_Classification/Geo_layout_codebase/Geo_LayoutLM-geo_code_latest_timeoptm_version_updated_apr22_sharing_backend/training_main/main/CS_Data/CS_annoation/data_in_funsd_format/dataset/custom_geo
task: ee_el
img_h: 768
img_w: 768

seed: 1
cudnn_deterministic: False
cudnn_benchmark: False

model:
  #n_classes: 40
  backbone: alibaba-damo/geolayoutlm-large-uncased
  config_json: ./configs/GeoLayoutLM/GeoLayoutLM_large_model_config.json
  model_ckpt: /model
  head: vie
  use_inner_id: true
  max_prob_as_father: true
  max_prob_as_father_upperbound: false

train:
  batch_size: 1
  num_samples_per_epoch: 120
  max_seq_length: 512
  max_block_num: 150
  max_epochs: 50
  use_fp16: False
  accelerator: cpu
  strategy:
    type: ""

  clip_gradient_algorithm: norm
  clip_gradient_value: 1.0
  num_workers: 1

  optimizer:
    method: adamw
    params:
      lr: 2e-5
      weight_decay: 0.01
      eps: !!float 1e-8
    lr_schedule:
      method: null #linear 
      params:
        warmup_steps: 200

  val_interval: 1  # in epochs

val:
  batch_size: 1
  num_workers: 2
  limit_val_batches: 1.0
  dump_dir: /home/ntlpt19/Downloads/MERGED_DATA/GEO_Latest/geolayoutlm_code_base_2/CI_EVAL/val_inference_files/dataset
  pretrained_best_type: linking # labeling, linking

pretrained_model_file: /datadrive/intelligent_trade_finance/Trained_Models/Extraction/CS/Best_Model/pytorch_model.bin

doc-type:
  CS: 
    
  COO:

  PL:

  IC:

  BOL: